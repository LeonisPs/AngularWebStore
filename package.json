{
    "name":"webstore",
    "version":"1.0.0",
    "engines": {
        "node": ">=24.11.1 <25.0.0"
    },
    "scripts": {
        "start": "cd BackEnd/build && node server.js",
        "prebuild": "cd Backend && npm run build",
        "build": "cd ClientSide && npx ng build"
    }
}
